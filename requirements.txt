# Core dependencies (Part I Hodge pipeline)
anndata>=0.10
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
statsmodels>=0.14
pyyaml>=6.0

# 3φ residual framework, NEMF screen, all-gene insertion
# (covered by core dependencies above)

# Optional: SALS-only DESeq2 + GSEA rank test (scripts/de_sals_only.py)
# pydeseq2==0.5.0  # uncomment if running DE analysis

# Optional: GPU acceleration for all-gene insertion (allgene_insertion.py)
# See https://pytorch.org/get-started/ for platform-specific install commands
# torch>=2.0

# Optional: Single-cell analysis (Part III diffusion pseudotime)
# scanpy>=1.10

# Optional: Part III LiNGAM causal inference
# lingam>=1.8

# Optional: Streamlit web app
# streamlit>=1.30
# plotly>=5.18

# Optional: MR analysis is in R, not Python.
# See: benchmark/mendelian_randomization/README.md for R package list
#   install.packages(c("TwoSampleMR", "coloc", "ieugwasr"))
